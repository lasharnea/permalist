# Railway (recommended)
# Set this in Railway Variables as: DATABASE_URL=${{Postgres.DATABASE_URL}}
DATABASE_URL=

# App port
PORT=3000

# Local fallback (used when DATABASE_URL is not set)
DB_USER=postgres
DB_HOST=localhost
DB_NAME=permalist
DB_PASSWORD=
DB_PORT=5432
